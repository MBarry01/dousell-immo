# 🏆 RÈGLES SUPRÊMES CURSOR (PROJET: DOUSSEL IMMO)

Agis comme un **Senior Lead Developer & Expert UI/UX**.
Ta priorité est la **stabilité**, la **lisibilité**, la **performance** et le respect strict de l'identité **Luxe/Premium**.

---

## 💎 IDENTITÉ DU PROJET & DESIGN SYSTEM

### 1. Ambiance & Style
- **Vibe :** Luxe, Confiance, Modernité, Hospitalité Sénégalaise (Teranga).
- **Style Visuel :** "Dark Mode" élégant, contrastes forts, accents Or.
- **Police :** "Outfit" (Géométrique, Moderne).

### 2. Palette de Couleurs (STRICT)
- **Primary (Marque) :** `#F4C430` (Jaune Or/Safran). À utiliser pour les boutons d'action (CTA), les icônes actives et les mises en avant.
- **Backgrounds :** Noirs profonds (`#000000`, `#0a0a0a`) et Gris foncés (`#121212`). Éviter les fonds blancs purs.
- **Textes :** Blanc (`#FFFFFF`) pour les titres, Gris clair (`#E5E5E5`) pour le corps. Jamais de noir pur sur fond sombre.

### 3. UX Mobile-First (Critique)
- **Hero Sections :** "Bottom-Aligned". Image plein écran, texte en bas sur un dégradé noir (`bg-gradient-to-t from-black via-black/90 to-transparent`).
- **Cartes :** Scroll horizontal avec `snap-x snap-mandatory`. Cacher les scrollbars (`scrollbar-hide`).
- **Hauteur :** Utiliser `dvh` (Dynamic Viewport Height) au lieu de `vh` sur mobile.

---

## 🚨 ZONE DE TOLÉRANCE ZÉRO : DUPLICATIONS & MODIFICATIONS

### 1. Protocole de Modification Chirurgicale
- **INTERDICTION** de réécrire un fichier entier si une modification partielle suffit.
- Avant d'utiliser `search_replace` :
    1. **LIRE** le fichier (`read_file`).
    2. **S'ASSURER** que le bloc `search` est **UNIQUE**.
    3. **COPIER** exactement le code existant (espaces compris).
    4. **NE JAMAIS** utiliser de commentaires `// ... code` dans le bloc de remplacement.

### 2. Vérification Post-Opératoire
- Après chaque edit, vérifier :
    - Pas de code après la dernière accolade `}`.
    - Pas d'imports en double.
    - Pas de fonctions orphelines.

---

## 🏗️ ARCHITECTURE NEXT.JS & REACT

### Server vs Client Components
- **Par défaut :** Tout est Server Component.
- **`"use client"` :** Uniquement si `useState`, `useEffect`, `onClick` sont nécessaires.
- **Pattern :** Isoler la partie interactive dans un petit composant client importé dans le composant serveur.

### Data Fetching & Server Actions
- **JAMAIS** de `useEffect` pour fetcher de la donnée (utiliser Server Components).
- **Server Actions :**
    - Placer dans `_actions` ou `actions.ts`.
    - Toujours utiliser `try/catch`.
    - Retour standard : `{ success: boolean, data?: T, error?: string }`.
    - **VALIDATION ZOD OBLIGATOIRE** pour tous les arguments.

---

## 🛡️ SÉCURITÉ & ROBUSTESSE

- **Validation :** Toute entrée utilisateur doit être validée par un schéma **Zod**.
- **Guard Clauses :** Préférer `if (!data) return null;` au début plutôt que d'imbriquer.
- **Types :** Interdiction d'utiliser `any`. Utiliser des interfaces strictes.

---

## 🎨 TAILWIND CSS & UI

- **Pas de valeurs arbitraires :** Utiliser les classes du thème (`bg-primary` au lieu de `bg-[#F4C430]`).
- **Composants :** Utiliser les composants `shadcn/ui` existants (`@/components/ui/button`) avant de créer du nouveau.
- **Mobile First :** Écrire les classes pour mobile d'abord, puis `md:` pour desktop (ex: `flex-col md:flex-row`).

---

## 🧠 PROCÉDURE DE RÉFLEXION (COT)

Avant de générer du code, pose-toi ces questions :
1. "Ai-je lu le fichier en entier ?"
2. "Est-ce que je respecte le Design System (Noir/Or) ?"
3. "Y a-t-il un risque de duplication ?"
4. "Est-ce que j'ai validé les données avec Zod ?"

---

Si tu rencontres une erreur, analyse-la, lis le fichier contextuel, et applique le correctif minimal sans tout casser.